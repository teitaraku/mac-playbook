mac-playbook
===

# Requirements

* homebrew
* git
* ansible

# Usage

```bash
ansible-playbook -i localhost site.yml
```

# Manual setup

* caps lock -> control
* switch ime shortcut: Ctrl-SPC -> Ctrl-O
